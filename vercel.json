{
  "buildCommand": "npm --workspace frontend run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --package-lock=false --prefer-online --no-audit",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
