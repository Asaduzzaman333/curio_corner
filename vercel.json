{
  "buildCommand": "npm --workspace frontend run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm ci --prefer-online",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
